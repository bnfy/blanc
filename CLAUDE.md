# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Blanc — a minimal Electron browser shell: custom-drawn "Island" chrome (a single floating command pill replacing the traditional tab strip + toolbar, per the Bowser Design System's Island Chrome handoff) with ad/tracker blocking wired in at the network layer, independent of Chrome's extension store and Manifest V3's `declarativeNetRequest` limits. Plus favorites, history, downloads, settings, slash commands, a Quick Switcher, private tabs, an explicit permission policy, and packaging config.

The app was renamed from "Bowser" to Blanc in July 2026 — its former name was inseparable from Nintendo's Mario villain, which didn't fit a serious indie browser's ambitions. `build.appId` in `package.json` deliberately stayed `me.bnfy.bowser` through the rename (only `name`/`productName` changed) to preserve macOS Gatekeeper/notarization identity and the auto-update chain for existing installs — don't "clean up" that mismatch, it's intentional. The design system's project name ("Bowser Design System", referenced throughout this file) is a separate Claude Design project the user maintains and wasn't renamed alongside the app; leave those references as-is.

**Current public baseline (Sep 2, 2026): v1.15.0.** It was published from `d0c2304` after the complete macOS arm64 signing/notarization, Windows exact-publisher/timestamp Authenticode, Linux/Windows packaged-payload, hardened-fuse, live microphone-plus-camera, authenticated checksum-manifest, SBOM, provenance, logged-out download, and fresh public AppImage launch/render gates passed. It expands Mahjong from three to eight solvable-by-construction layouts, rotates the Daily board across all eight, adds device-local records and streaks, resumes unfinished play across tabs, and makes Shuffle undoable. The adjacent public v1.14.0 → v1.15.0 macOS and Windows in-app updater handoffs remain required follow-ups. Do not recapture launch demo/gallery media for v1.15.0 unless it is selected as the final launch release; refresh release-bound media once, from the final selected launch release, after its required evidence is complete. Evidence lives in `docs/release-incidents/2026-09-02-v1.15.0.md`; the complete v1.14.0 record remains in `docs/release-incidents/2026-09-02-v1.14.0.md`. A directly launched installer is not an updater-handoff test — the handoff must begin inside the old packaged Blanc, discover the public updater metadata, download the matching artifact, and invoke Restart Now.

**Official launch schedule (Aug 30, 2026 decision): September 7–10, 2026.** PR #251 / merge commit `dfe600a` retired the August 31–September 3 calendar and opened a bounded prelaunch backlog-cleanup window; PR #252 / merge commit `c8a14b6` records the owner's decision to ship multiple immutable post-v1.10.0 releases before launch. Product merges stop Thursday, September 3 at noon ET. Every release in the train must complete truthful publication, macOS/Windows/Linux, authenticated-manifest, download, and dated-incident evidence, and each macOS/Windows updater handoff must begin in the immediately preceding public version—a direct installer or a jump over an intermediate release does not prove the real chain. Intermediate versions may be intentionally superseded without their own 48-hour launch soak, but known regressions stop the train. The final launch release and release-bound assets/copy must be complete by Friday, September 4 at 3:00 p.m. ET; only that final release starts the fresh verified soak of at least 48 hours, and any replacement restarts the clock. Launch execution is Monday listings, **Tuesday, September 8 Show HN**, Wednesday Reddit, and Thursday Product Hunt. The reschedule is not blanket approval to merge backlog work, publish a release, or waive platform/evidence gates. Once the final release-backed freeze is recorded, `origin/main` may advance through the Show HN post only for launch evidence, launch copy, and their regression guards. Missing the release cutoff or soak moves launch again; it never shortens the soak.

**Open-source baseline (Aug 30, 2026): MIT is the deliberate direction.**
Blanc is open source, not merely source-available. Commit `b49261a` adopted the
MIT License for Bananify Creative-owned software, documentation, and media and
aligned the repository, FAQ, and Terms; the canonical site was deployed from
that exact commit. Do not reintroduce `UNLICENSED`, "source-available, not open
source", or language forbidding modification or redistribution unless the owner
explicitly reverses this decision. Open source is part of Blanc's trust,
adoption, auditability, and grant strategy. Patron should be framed as support
for Blanc and the best official experience, not as a source-code restriction.

The grant is intentionally bounded. The Blanc and Bananify Creative names,
logos, and other identity assets listed in `ASSET-LICENSE.md` remain reserved;
MIT does not grant trademark rights. Upstream files and third-party material
retain their own terms, recorded in `THIRD-PARTY-NOTICES.md` and the packaged
`src/THIRD_PARTY_NOTICES.txt`, including EasyList/EasyPrivacy, 1Password, Inter,
JetBrains Mono, and Lucide. Never describe the whole repository as blanket MIT
or remove those carve-outs. Public v1.11.0 was the first packaged release under
this licensing baseline. It and later releases carry `LICENSE`, both notice
files, `ASSET-LICENSE.md`, both OFL texts, and the pinned EasyList/EasyPrivacy
sources inside `app.asar`, as required by `package.json` and the packaged
compliance gate.

**Marketing claims must be release-backed.** Before writing, approving, or publishing any article, social post, ad, demo caption, outreach message, comparison, or image text about Blanc, follow `docs/marketing-claims.md`. Verify shipped capability against the current public release tag and its release evidence, not merely the working tree, a design spec, a plan, a mockup, or aspirational copy. Treat external-product claims the same way: use a current first-party source and distinguish sourced fact from inference. In particular, Blanc does not understand assignments, detect semantic task boundaries, automatically organize tabs by meaning, or provide an AI/agent browser; Named Groups and Named Workspaces are user-directed features. Do not publish while any material claim is unsupported or ambiguous.

## Commands

```
npm install
npm start                       # run in dev (electron .)
npm run dist                    # installable build: macOS dmg/zip, Windows NSIS, Linux AppImage
npm run dist:dir                # quick unpacked build in dist/, no installer
npm run release                 # scripts/release.sh: clean, build, sign, publish to GitHub Releases
```

After a release, `npm run site:deploy` is the production Cloudflare Pages
command. It deliberately includes `--branch=main` because the required clean
release checkout is detached at `origin/main`; without that flag Wrangler
creates a `HEAD` preview and leaves `blancbrowser.com` unchanged. A successful
post-release deployment is not complete until `wrangler pages deployment list
--project-name=blancbrowser` shows the expected source SHA as `Production` on
branch `main` and the canonical changelog shows the new version.

There is no linter configured and no plain `npm test` — the real commands are `npm run test:unit` (node --test over `test/unit/`), `npm run test:acceptance:dry` / `test:acceptance:desktop` (Cucumber scenarios in `spec/acceptance/`, the latter launching the app via Playwright), `npm run test:oauth:desktop`, and `npm run substrate:check` (tokens/settings/copy/adblock generated files match their sources).

`npm run release` bumps nothing itself — bump `version` in `package.json` (and consider the `electron` devDependency, since Chromium can't be swapped out of a running app) *before* running it. It shells out to `scripts/release.sh`, which authenticates via the `gh` CLI's own cached session (no `GH_TOKEN` needed locally), builds unpublished, then uses `gh release create` directly instead of electron-builder's own GitHub publisher — that publisher races its per-artifact upload tasks against each other on first publish for a tag and can leave a release missing `latest-mac.yml` or a blockmap; creating the release once with the complete macOS asset set avoids that. **Released versions are immutable:** the script checks local/remote tags and GitHub releases before building and fails if the version already exists; always bump to a new version, never overwrite assets. It also refuses dirty desktop release sources and wipes `dist/` before building so artifacts correspond to committed code and stale files never linger.

**Notarization:** the build step is wrapped in `op run --env-file=.env.1password --no-masking -- ...`, pulling `APPLE_ID`/`APPLE_APP_SPECIFIC_PASSWORD` from the 1Password item **"Apple Notarization"** (vault Dev, `username`/`password` fields) plus a literal `APPLE_TEAM_ID` — electron-builder's `@electron/notarize` integration auto-detects those three env vars. The hardened runtime keeps only Electron's required JIT/unsigned-executable-memory allowances; library validation stays enforced everywhere except `Blanc Helper (Plugin).app`, whose sole exception loads 1Password's separately signed native bridge (see the password-manager section). `release.sh` requires `op` and fails closed if notarization cannot run — a public release never falls back to an unnotarized build. A signed-but-unnotarized build gets quarantined and blocked by Gatekeeper the moment it is downloaded.

**Release-operator protocol (mandatory):** `release.sh` supports native Terminal (`BLANC_RELEASE_OPERATOR=terminal`) and an explicitly approved out-of-sandbox agent PTY (`BLANC_RELEASE_OPERATOR=agent`). During v1.5.1 the Codex safety layer rejected agent mode even after approval. Do not retry that dead end: after one rejection, explain it once, obtain approval for native Terminal, and launch the complete terminal-mode command there automatically so the user need not type or copy it. Keep it foreground, visible, and interactive. The script itself runs the exact fail-fast command `OP_BIOMETRIC_UNLOCK_ENABLED=true op signin` before build work and later uses forced desktop integration for `op run`; do not invent another sign-in flow, and cancel if `op` offers to add an account manually. A sandboxed `gh auth status` can falsely report an invalid token because it cannot reach the macOS keyring: retry outside the sandbox and use the connected GitHub app or working Git credential before asking the user to reauthenticate; do not send the user through `gh auth login` unless every existing path actually fails. Read `docs/release-verification.md` immediately before every release. Complete the fresh Safari Sigstore page immediately. During notarization inspect process names only, never full arguments, because `notarytool` includes the app-specific password in argv. Never rerun an immutable release after its tag or draft exists. The v1.5.1 lessons are recorded in `docs/release-incidents/2026-08-17-v1.5.1.md`.

**Release completion and takeover protocol (mandatory after v1.15.0):** Publishing the GitHub release is not the end of the release. Keep `scripts/release.sh` as the single owner of the signed macOS build, immutable tag and draft, native workflow dispatch, complete-manifest generation, fresh Safari Sigstore authorization, publication, logged-out asset checks, and changelog refresh; do not manually duplicate or race those stages. If taking over a live release from another agent, first reconcile the foreground Terminal process, tag SHA, GitHub release state and assets, and the exact native run. If the release is already public and `SHA256SUMS.sigstore.json` exists, the Sigstore authorization already succeeded—do not open another approval or rerun the release. Continue with independent post-publication evidence from a fresh temporary download: run the complete manifest validator, `shasum -a 256 -c SHA256SUMS`, and `cosign verify-blob` against the pinned identity and issuer. For macOS, mount the published DMG read-only, assess the contained `Blanc.app` with Gatekeeper, run strict deep `codesign` verification, confirm the expected Developer ID, and validate the ticket stapled to the app; the documented gate does not require a separate ticket on the DMG container. Then dispatch `.github/workflows/prerelease-smoke.yml` against the exact release tag with `release_version` set and wait for the public AppImage plus Linux and Windows DNS jobs to pass. Only then update the public baselines, dated release incident, `site/src/data/releases.json`, the next release's default `MIGRATION_BASE_VERSION`, and its unit-test assertion from a clean isolated worktree; run the changelog check and full site/SEO build, commit, and push. Deploy with `npm run site:deploy`, verify Cloudflare reports that exact commit as `Production` on branch `main`, verify the canonical homepage and changelog, append that deployment evidence to the incident, and push the follow-up record. Leave adjacent macOS and Windows updater handoffs explicitly pending until each begins inside the immediately preceding public Blanc, discovers the public metadata, downloads the update, and invokes **Restart Now**; a direct installer launch is never a substitute.

**Provisioning profile (Touch ID passkeys):** the mac build embeds `build/embedded.provisionprofile` (wired via `build.mac.provisioningProfile`) because the WebAuthn `keychain-access-groups` entitlement is *restricted* — even for the app's own `TeamID.BundleID` group, AMFI SIGKILLs an app carrying it without a profile that authorizes it, and helpers can't carry a profile at all (so the inherit entitlements must never list the group). The profile is only honored when it embeds the **exact certificate** the build is signed with; the Apple account has several identically-named Developer ID Application certs, so the signer is made deterministic and verified twice. `build.mac.identity` pins the signing certificate **by SHA-1 fingerprint** (electron-builder substring-matches the qualifier against `security find-identity` lines, so a fingerprint selects exactly one identity — update the pin when the cert rotates). `scripts/preflight-mac-signing.mjs` (npm `predist`/`predist:dir` + `release.sh`) then fails the build early unless *every* certificate electron-builder could select — the pinned identity, a `CSC_LINK` p12 (parsed and validated, never skipped), or unpinned Developer ID identities — is embedded in the profile, and unless the profile is an unexpired, all-devices (`ProvisionsAllDevices`, never `ProvisionedDevices`), `Platform: OSX` Developer ID profile — a device-listed development-type profile would launch only on registered Macs, bricking every other install through auto-update, while end-user devices need nothing installed for the correct profile type (it ships inside the bundle). Finally `scripts/after-sign-verify.js` (`build.afterSign`) checks ground truth on every packaged app: the certificate that *actually* signed `Blanc.app` must be in the bundle's embedded profile (itself byte-identical to the repo's) with the WebAuthn entitlement present, throwing before any dmg/zip exists. Regenerate the profile on the developer portal whenever the signing cert rotates — and note every current Developer ID cert expires 2027-02-01 (G1 CA cap), so cert *and* profile rotation land together before then. `test/unit/webauthn-packaging.test.js` guards the static wiring cross-platform.

**Windows & Linux:** `release.sh` builds macOS locally, then dispatches `.github/workflows/release-windows-linux.yml` and watches it to completion. Native artifacts stay on an unpublished draft until every platform succeeds. Windows signing is fail-closed: complete Azure Trusted Signing configuration is tried first, a complete `CSC_LINK`/`CSC_KEY_PASSWORD` pair is the Authenticode fallback, and missing or partial signing configuration fails the job. The installer and unpacked app must have a valid timestamped signature whose exact subject equals `WINDOWS_EXPECTED_PUBLISHER`; `windows-signature.json` binds that observation to the installer SHA-256. Linux AppImages have no OS-level publisher signature, so the authenticated release manifest is their publisher check.

Before a Windows hotfix is released, agents can build a private, signed installer from the candidate branch with `gh workflow run release-windows-linux.yml --ref <candidate-branch> -f mode=validation -f platform=windows`. Validation mode checks the normal signature, timestamp, fuses, and files and uploads the installer, blockmap, signature record, and `latest.yml` only as a three-day Actions artifact. Installing that EXE proves packaged behavior, but launching it directly while Blanc is open does **not** prove the `electron-updater` handoff. That claim requires Blanc itself to discover the staged metadata, download the exact candidate, and invoke **Restart Now**. Obtain explicit affected-machine confirmation before merge/tag/release. If the owner waives a gate, state the missing evidence and risk, obtain written approval, and record the waiver rather than claiming the check passed. Normal releases dispatch with `mode=release`.

The Azure branch requires every tenant/client/account/endpoint/profile/publisher value before it is attempted; partial Azure configuration may use the complete CSC fallback but may never reach unsigned output. AppImages for the runner's default architecture have no `-x86_64` suffix and carry their block map internally. The release gate verifies Electron fuses in every packaged executable, emits a CycloneDX SBOM and `SHA256SUMS`, signs the checksum manifest with Sigstore, and requires exact OIDC identity/issuer verification before publishing. GitHub Actions are commit-SHA pinned; native artifacts receive GitHub provenance attestations.

The desktop ad blocker compiles only the bundled, hash-verified EasyList + EasyPrivacy snapshots in `adblock/sources/`; it makes no live filter-list request at startup and strips remote scriptlet resources. The compiled engine is cached in userData as `adblock-engine.v<N>.bin`; delete it to force a rebuild from the same bundled snapshot. Updating the lists is a reviewed release input: update `pinned.json`, run `npm run adblock:build`, and commit the generated snapshot/hash changes together.

The pinned blocker files are byte-verified, so `.gitattributes` must keep `adblock/sources/*.txt`, `adblock/sources/pinned.json`, and `adblock/generated/*.json` at `eol=lf`. Without that, a Windows checkout converts them to CRLF and every packaged launch correctly fails closed with **Blocking could not start**. The native Windows workflow must run `npm run adblock:check` before electron-builder; never remove or bypass that gate. Every electron-builder package also runs `scripts/verify-packaged-adblock.js` from the cross-platform `afterPack` hook, comparing the actual `app.asar` payload byte-for-byte with the verified release inputs before signing. Release workflows repeat that packaged-payload check explicitly for macOS, Windows, and Linux.

Auto-update (`electron-updater` against GitHub Releases) only runs in packaged builds — `npm start` skips it entirely. Releasing means bumping the `electron` devDependency (it tracks Chromium stable) and `version` in `package.json`, since Chromium itself can't be swapped out of a running app.

## Architecture

**Many `BrowserWindow`s, each with many `WebContentsView`s.** The window's own `webContents` renders `src/renderer/index.html`/`styles.css` through the exact-allowlist `blanc-chrome://` scheme — the chrome **strip**: a slim (68px) band holding the resting island pill (back/forward, the active group's tab dots with a capped overflow, favicon, domain, the Blanc Blocker count, private chip, and a trailing reload/favorite/close-tab/downloads action cluster) and window controls. Permission prompts render in their own small transparent `WebContentsView` (`permission.html`/`permission.js`, the third `blanc-chrome://` document), attached bottom-center over the page only while prompts are pending — the strip only paints the top band, and anything beside the island competes with it. The whole resting pill is scaled up in one place with `zoom` on `#islandPill` (kept in lockstep with the `--strip-h` geometry token); each anonymous tab dot blooms into a disc showing that tab's favicon on hover/focus (`.dot-peek`, reusing the pill's favicon rendering) so you can tell which tab you're switching to before you click. Each tab is a separate `WebContentsView` added as a child of the window's `contentView` at `y = chromeHeight`; only the active tab's view is attached, so switching tabs is remove-one/add-another rather than destroying anything — but an *idle background* tab is a separate matter, and its renderer is deliberately discarded (see **Quiet Tabs** below). `src/main/main.js` owns all of this: the `tabs` Map + `tabOrder` array are the single source of truth, and both chrome renderers only ever reflect `tabs:updated` broadcasts.

**Quiet Tabs.** An idle background tab's renderer is deliberately discarded to reclaim its memory (~148 MB/tab measured on Electron 43), and rebuilt when the tab is activated again. Policy is pure and unit-tested in `src/main/tab-sleep.js` (`sleepCandidates`, `trimSnapshot`, `TAB_SLEEP_DELAY_MS`) — no `require('electron')` in that file. View construction and the whole per-tab listener set live in `src/main/tab-view.js` (`createTabView`, `wireTabView`, `initTabView`), which is also the single home of the private-session choice. `main.js` owns `sleepTab`/`wakeTab`, the 30 s sweep, and the wake generation (`tab.wakeGeneration`) that suppresses history and failure handling across every hop of a redirect chain. **`liveContents(tab)` is the only correct liveness check**: after `webContents.close()`, `view.webContents` can read back `undefined`. The main-process-only `sleepSnapshots` Map retains bounded navigation recovery data and must never move onto the tab record or cross IPC, disk persistence, sync, or crash reporting; snapshots may contain page state and POST bodies. During app shutdown, `closeTab` must never select and wake a surviving quiet tab while Electron tears down the native window. The dirty probe protects changed form controls across the frame tree and Chromium's unload objection. It deliberately does **not** treat non-empty `sessionStorage` as user work, because ordinary sites create site-owned keys on load and that blanket rule makes the feature inoperative. Instead, a storage-bearing tab keeps its WebContentsView in the snapshot Map, discards only its provably-exclusive renderer, and reloads that same WebContents so Chromium's browser-process sessionStorage namespace survives; any beforeunload listener or shared/uncertain renderer PID leaves it awake. `session.json` has an optional `meta: [{title, favicon}]` array parallel to `urls`, written into each v2 `windows[]` entry, never the five-key flat rollback mirror; `version` is `2`. Restored tabs are born quiet and only the selected tab loads. Internals say `asleep`/`tabSleep`/`sleepSnapshots`; every user-visible and assistive string says **quiet**, except the fixed `/sleep` command. The device-local delay is Settings → General → Quiet inactive tabs (`off|30m|1h|6h`, default `1h`, deliberately outside `SYNCED_KEYS`); Off stops future quieting and never wakes an existing quiet tab or drops its snapshot.

**Reopen Closed Tab.** Closing a tab records a per-window closed entry with three internal restore tiers: for ~30 s the closed tab's live `WebContentsView` is parked (same view re-attached on ⌘⇧T, no re-fetch, JS and POST state intact), then it degrades to a `trimSnapshot()` restore (back stack, scroll, group, pin, slot), with URL-only as the floor. Never expose tier jargon such as **held** in user-visible copy. Pure policy lives in `src/main/closed-tabs.js` (`holdEligibility`, `sanitizeSnapshot`, `buildTabEntry`/`buildGroupEntry`, `expireHolds`, `expireEntries`, `projectEntries` — no `require('electron')`); `main.js` owns parking (`parkTabView`), the held-state firewall (a parked page keeps executing, so it gets deny-all `setWindowOpenHandler`, main-frame navigation refusal, crash/destroy downgrade observers, and blanket permission denial via `heldWebContents` + `setHeldRequesterCheck` in permissions.js), restore (`reopenEntry`, the `adoptView` seam in `createTab`), and disposal. One entry per user action: `closeGroup` captures first and records a single group entry restored whole at its cluster index. Closed entries are the same class of secret as `sleepSnapshots` — snapshots and view refs never cross IPC/disk/sync; renderers see only `{id,title,favicon,tabCount}`. The panel offers per-entry Forget and Clear all; either must immediately destroy a parked view. Entries also expire after one hour and remain capped at 25. Capture-anchored, prompt-pending, loading, quiet, and opener-family tabs are never held (Tier 1); private tabs are never recorded at all. Nothing is persisted, and on macOS dock-close the held view is destroyed while snapshot entries survive until their normal expiry.

Per-window state (workspace, overlay, shield popover, utility sheet, focus
reclaim, permission prompts) lives on a window-runtime record
(`src/main/window-runtime-registry.js`) — one per native browser window — resolved
via AsyncLocalStorage bindings and sender-derived IPC; the `tabs` Map stays
process-wide with per-tab `runtimeId`. `session.json` is versioned (v2 +
a v0 mirror of the focused window so a rollback to 1.0.x keeps tabs).

**Local profiles.** `profiles.json` defines the permanent Personal identity plus bounded named profiles. Personal keeps every shipped root JSON file and Electron's default session, so upgrades move no existing data. Named profiles store Favorites, history, download metadata, and remembered permissions under `profiles/<opaque-id>/`; their normal tabs use `persist:blanc-profile-<id>` and private tabs use `private-browsing-<id>`. Settings, Supporter, telemetry, and network/presentation preferences remain device-level. Profile Sync and remote-tab presentation are Personal-only. Deletion is exact-name-confirmed and crash-resumable through the device-level `profile-deletions.json` marker; it closes the profile's windows, clears its Electron sessions, drops cached store/download state before removing its directory, and removes every saved workspace. Downloaded files remain on disk.

**The island's expanded states live in a separate always-on-top overlay `WebContentsView`** (`src/renderer/overlay.html`/`overlay.js`, transparent via `setBackgroundColor('#00000000')`), attached to the window's `contentView` only while something is showing, so panels float *over* the web content instead of growing the strip and shifting it down. Main owns the mode (`overlayMode`): `'panel'` — the command bar expanded in place at the pill's anchor; `'palette'` — the same panel summoned by ⌘L, on the same anchor as `'panel'` but over a scrim (it used to sit 120px lower, which read as a detached window rather than the pill opening); `'find'` — the find capsule, whose overlay bounds are kept *tight* around the capsule so the rest of the page stays clickable; `'shield'` — the site-protection popover opened from the pill's shield chip (fixed `calculateShieldBounds` region below the strip, right-aligned to the chip; site-scoped toggle reusing the /allow-ads//block-ads command paths; dismissed like panel/palette plus on tab switch and site-changing navigation — the toggle's own reload is same-site and keeps it open). `showOverlay`/`hideOverlay` in main are the only mutators; the overlay renderer just applies `overlay:show`/`overlay:hide`. Guards that matter: a tab view attached while the overlay is open must be re-stacked below it (re-`addChildView(overlayView)` in `setActiveTab`), Escape dismisses via main's `before-input-event`, and overlay `blur` dismisses panel/palette (not find) — but is ignored while a blank-tab focus reclaim is pending, or the reclaim dance re-opens/closes in a loop. The panel's list area shows the tab switcher at rest, slash commands when the input starts with `/`, and the Quick Switcher (loose substring/in-order matching across open tabs, favorites, history) while typing anything else.

**Private tabs** (`/private`, ⌘⇧N, `private` on main's tab model): never recorded to history, excluded from session persistence, sync, and reopen-closed-tab, inherited by `window.open`/context-menu children, and opened to `blanc://newtab/?private=1`. Their views share the non-persistent `private-browsing` session, isolated from the normal profile; its cookies, storage, cache, service workers, HTTP auth, and permission decisions disappear when Blanc quits. Completed private-download metadata is kept only in memory, while the file the user saved remains on disk. The same permission, blocker, compatibility, and WebAuthn policies are installed on both sessions. While the active tab is private, the chrome uses its private theme and quick-exit chip.

**Tab groups** (per the Island Tab Groups exploration in the Bowser Design System): main owns `groups` (`{id, name, collapsed}`, display order = cluster order) alongside `tabs`; each tab carries `groupId` (or null). Groups have **names, not colors** — lowercase mono labels. A group exists only while it holds tabs (`pruneEmptyGroups`); creation happens through `/group <name>` (find-or-create) or the tab context menu's **Move to Group ▸ → New Group…**, which opens the ⌘L panel prefilled with `/group ` and binds that one run of the command to the right-clicked tab (`overlay:begin-group` → `pendingGroupTabId` in overlay.js — cleared on use, dismissal, or editing the input away from `/group`). Membership moves via the same submenu (existing groups as radio items, **Remove from Group**), `/ungroup`, or `/close-group`. There is deliberately no inline per-row group chip/picker any more — it crowded rows into truncation; grouping lives in the right-click menu (`tab-context-menu-model.js` / `tab-context-menu.js`, shared by the resting pill and the ⌘L tab rows). The pill now renders **only the active tab's group** as dots (capped at 8, with a quiet `+N` that opens the panel). It does **not** name the group — group identity lives in the ⌘L panel (group headers) and in which tabs the dots represent. Pinning is group-local: a grouped pin stays in that group and leads its rows/dots, while only ungrouped pins use the standalone pinned section in the ⌘L panel. Other groups also live in that panel behind per-group headers (click folds/unfolds; collapsed groups show an "N tabs tucked away" row). The Quick Switcher matches group names (ranked above tabs; picking one focuses the group), and ⌘1–9 change meaning when groups exist: nth *cluster* (group's first tab, unfolding it) instead of nth tab. A plain new tab (⌘T, panel row, `/new`) always launches ungrouped, regardless of the active tab's group — join a group afterward via the row's right-click menu. `window.open`/context-menu children are the exception: they still inherit the opener tab's group, since a link opened from a grouped tab is presumed to belong with it. New private tabs are never grouped. `session.json` persists `groups` plus a `groupIds` array parallel to `urls`; groups referenced only by private tabs stay out of the file.

**The chrome documents only load their HTML/CSS once, at window creation** (`rt().window.loadURL(CHROME_INDEX_URL)` + `createOverlay()` in `createMainWindow()`). Editing `src/renderer/index.html`, `overlay.html`, `styles.css`, or either chrome renderer requires relaunching the app to see the change — `Cmd/Ctrl+R` reloads the *active tab's* `WebContentsView` (which re-fetches `blanc://.../pages.css` fresh on every navigation), not the chrome. Don't rely on a plain reload to verify a chrome-level change; restart `npm start`.

**`blanc://` internal pages** (`newtab`, `bookmarks`, `history`, `downloads`, `settings`) are served by `src/main/pages.js` over a privileged custom scheme instead of `file://`, so they get a real origin and ordinary web content can never link into local files. The handler resolves every request through `path.basename()` before joining it to `PAGES_DIR` (`src/renderer/pages/`) — **it can only serve flat files directly in that one directory, no subdirectories and no `../` traversal.** Any asset (`pages.css`, `icon.svg`, a font file) that `pages/*.html` needs must live flush inside `src/renderer/pages/`. The five utility pages (`bookmarks`, `history`, `downloads`, `settings`, `shortcuts`) **never open as tabs** — main routes them (via `src/main/utility-pages.js`'s `isUtilityUrl`, the single classifier checked at `openInternalPage`, tab `will-navigate`, `createTab`, the window-open handler, and typed navigation) into the **utility sheet**: a transparent always-below-the-island `WebContentsView` whose page draws its own scrim + card (`body.sheet` in pages.css + the shared `sheet.js`). Esc, scrim click, and the nav ✕ (strictly-guarded `pages:surface:close`) dismiss; tab switches/creation and summoning ⌘L dismiss; outbound clicks open real tabs; session restore filters stale utility tabs through `session-restore.js`'s zipped filter. `blanc://mahjong` (mahjong solitaire) is a KNOWN page served flat like the others but deliberately NOT a utility page — it opens as an ordinary tab, is also embedded as the fifth start-page layout (`mahjong` in `NEWTAB_LAYOUTS`, an iframe permitted by newtab.html's `frame-src`), and its engine/page split is `mahjong-engine.js` (pure, unit-tested) + `mahjong.js` (DOM, timer, localStorage best). The newtab page is the **"ledger" start page** (direction 1a of the Start Page exploration in the Bowser Design System): date line, "Where to?", favorites, tab groups ("pick up where you left off" — clicking one focuses that group), and a footer with the weekly blocked count + ⌘L hint. There's deliberately no mascot on this page — an earlier version had an animated pixel-art dog sprite here, retired along with the "Bowser" name as part of the July 2026 rebrand; don't reintroduce one without being asked. Live group state and the blocked counter cross from main.js via the `startPage` hooks passed to `setupPages` (`pages:start:data` / `pages:start:focus-group`).

**Mahjong sound cues.** `mahjong-sound.js` synthesizes the game's quiet Web Audio cues locally and lazily after a user gesture; do not replace it with remote or bundled media without a CSP/privacy review. `mahjong.js` owns the accessible sound toggle and stores its device-local preference in the Mahjong origin's `localStorage`, which stays ephemeral in private tabs.

**Security posture:** the chrome window, the overlay view, and every tab's `WebContentsView` run `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`. Three separate preload scripts, deliberately asymmetric:
- `src/main/preload.js` — the rich `browserAPI` bridge (tab lifecycle, window controls, island data + command actions). Attached only to the chrome window and the overlay view.
- `src/main/tab-preload.js` — the minimal `bowserPages` bridge, exposed only when `window.location.protocol === 'blanc:'` (re-checked on every navigation, so a tab that leaves an internal page loses it). The main process still independently re-verifies `event.sender.getURL()` on every `pages:*` IPC call in `pages.js` — the client-side check is not trusted alone. `bowserPages`/`bowserAuth` (the analogous bridge for the basic-auth dialog) kept their names across the rebrand — they're internal-only identifiers, same rule as the Favorites/`bookmarks` split below.
- `src/main/chrome-compat-preload.js` — an unprivileged session-wide `window.chrome.app`/`csi`/`loadTimes` compatibility shim. It is registered through `session.registerPreloadScript` so both ordinary tabs and Chromium-adopted OAuth children receive it without replacing the child's `webPreferences.preload` (which severs `window.opener`). It exposes no IPC or application data.

**IPC channel namespaces:** `tabs:*` (tab lifecycle, via `browserAPI`), `pages:*` (internal pages' guarded data API, via `bowserPages`), `window:*` (minimize/maximize/close), `chrome:*` (strip/overlay → main: layout height, open-island/open-find, and island data + command actions — history-list/favorites-list/history-clear/adblock-toggle/adblock-exempt-active/cycle-theme), `overlay:*` (main ↔ overlay show/hide/close), `fill:*` (main ↔ the 1Password fill-status capsule: fixed-kind show/hide plus request-id-echoed verb replies — never any credential, vault, or page-derived string).

**Address bar focus** is intentionally intricate (`tabsWantingAddressBarFocus`, `reclaimAddressBarFocus()` in `main.js`) because a blank new tab needs the island panel's address input focused rather than its empty page content, and `WebContentsView` focus can settle asynchronously after Electron's own focus/navigation callbacks — hence the reassert-on-next-tick pattern rather than a single focus call. `focusAddressBar()` now means "show the overlay panel and focus its input".

**Theming:** CSS custom properties in `:root` (+ a `prefers-color-scheme: dark` override, + an explicit `:root[data-theme="private"]` scope), with the same token names duplicated across `styles.css` (chrome strip *and* overlay — one shared stylesheet) and `src/renderer/pages/pages.css` (internal pages) — separate files by necessity given the flat-serving constraint above, kept in sync by hand, not shared/imported. `nativeTheme.themeSource` is driven by Settings → Appearance (also cycled by `/theme`), so system/light/dark propagates to chrome, internal pages, and web content together, no restart.

**CSP is declared per-file**, not centralized: `index.html`, `overlay.html`, and each `pages/*.html` file carries its own `<meta http-equiv="Content-Security-Policy">`. Chrome/internal-page fonts and assets are bundled locally; their CSPs do not allow remote images or fonts. Adding an external resource requires a narrow per-document policy change and a privacy/data-flow review.

**Ad blocking** (`src/main/adblock.js`): the `@ghostery/adblocker-electron` engine is installed in both regular and private sessions from the bundled verified snapshot. Request blocking, declarative cosmetic CSS, and bundled page-context scriptlets remain; `adblock-scriptlets.js` isolates each scriptlet's declarations to prevent cross-scriptlet helper corruption. Toggling in Settings updates both sessions; blocked-request counts are per-tab and coalesced before reaching chrome.

**Capture indicator** (`src/main/capture-state.js`, `capture-preload.js`, `capture-mainworld.js`): a window-wide pill chip + popover showing live mic/camera use. The only off→on signal is the main-process permission grant (`setCaptureGrantObserver` in permissions.js); page-world instrumentation refines toward off via per-frame snapshots/settlements over `capture:report` — display state, not security truth (macOS's system indicator is the malicious-page backstop; a defeated patch fails stuck-on, never silently-off). Main-frame-only by spike-proven necessity (session preloads don't reach iframes without `nodeIntegrationInSubFrames`, which stays off — the main-world source therefore lives inline in `capture-preload.js`, and `capture-mainworld.js` re-exports the shipped bytes for the vm tests). Auxiliary `window.open` popups are capture surfaces in a process-wide registry that survives macOS window close/reopen. Capturing tabs are excluded from Quiet Tabs (`tab.capturing`, re-checked inside `sleepTab`'s final guard). Never persisted or synced; only the `tabs:updated` projection (`capture`, `captureChip`, `capturePopover`) leaves main. Capture IPC crosses unbound native boundaries, so its broadcasts rebind the window runtime (`scheduleCaptureBroadcast`) — same rule as downloads.js. Design: `docs/superpowers/specs/2026-08-13-capture-indicator-design.md`. The same preload also carries the **truthful `permissions.query` shim**: main-frame mic/camera queries answer `granted`/`denied`/`prompt` from Blanc's stored decisions over a sender-validated bridge (`capture:permission-query`), so preflighting sites ask instead of declaring the device blocked — display truth only; Electron's strict check/request handlers are untouched, and any bridge failure falls back to the strict query. Never map undecided to `true` in `setPermissionCheckHandler` (reviewed P0, reverted). Spec: `docs/superpowers/specs/2026-08-13-permissions-query-truthful-state.md`.

**No extension runtime or built-in password manager — deliberately.** The former extension runtime caused native crashes, required an unsandboxed chrome window, and imposed a GPL constraint, so it remains removed, and Blanc stores no credentials of its own. What did ship (v1.9.0, macOS-only) is the **1Password login fill**: explicit-invoke only (⌥⌘P, View → Fill Login from 1Password, `/1password` — never automatic), reading the user's installed 1Password app through `@1password/sdk` (pinned 0.5.0) inside the **Blanc Credential Broker** Electron utility process. The credential path is renderer-free — candidate usernames and credentials never cross renderer IPC (native dialogs and a native menu picker only). Only `Blanc Helper (Plugin).app` carries the `disable-library-validation` entitlement, solely to load 1Password's separately signed native bridge; signed-build verification fails if that exception appears on Blanc or any other helper. Windows and Linux expose no setting, menu item, shortcut, command, or IPC for it and cannot create the broker. Its in-flow UI (prompts, errors, confirmations) is the **fill-status capsule** — a fourth `blanc-chrome://` document with its own narrow preload receiving only fixed message-kind ids, credential-free by contract; the multiple-match picker stays a native menu, and the pill's ambient key-glyph hint comes from a structure-only page probe that never reads values and never contacts the SDK unprompted. Canonical doc (setup, security boundary, release gates, frozen names `onePasswordEnabled`/`onePasswordAccount`): `docs/1password-integration.md`. Electron Touch ID WebAuthn still creates Blanc-owned, device-bound Secure Enclave passkeys through `src/main/webauthn.js`; it does not read existing third-party credential-manager passkeys. On startup, `main.js` still performs a one-time purge of extension profile state left by old versions, deliberately preserving ordinary websites' Service Worker data.

**Naming: "Favorites" vs. `bookmarks`.** The user-facing feature is called **Favorites** (heart icon) per the Bowser Design System — but every internal identifier deliberately stays `bookmarks` (`bookmarks.js`, `bookmarks.json`, `pages:bookmarks:*` IPC, the `blanc://bookmarks/` URL, `pages/bookmarks.html`). Change labels/copy only; don't rename the internals, and don't "fix" the mismatch in either direction.

**Persistence** is one `JsonStore` per feature (`src/main/store.js`): JSON in `userData`, loaded synchronously once and normally saved on a 250ms debounce. Writes use an owner-only temporary file, fsync, and atomic replacement; legacy files are tightened to mode 0600 where supported. `settings.json`, `bookmarks.json`, `history.json` (capped at 5000), regular `downloads.json` (capped at 200), and `adblock-stats.json` remain local. Sync's retained derived key is stored only as an OS-credential-service-wrapped `protectedKey`; legacy plaintext keys migrate fail-closed. Because `userData` derives from `productName`, the permanent one-time Bowser→Blanc packaged-profile copy must remain so existing users keep their local data.

**Telemetry** (`src/main/telemetry.js`): bounded usage measurement, **on by default and opt-out** through the fresh-profile privacy choices or Settings. A fresh profile must save the presented choice before any event; existing profiles retain their saved setting and are not re-asked. Packaged builds only; fire-and-forget. Every event carries `{installId, sessionId, version, platform, arch, osVersion}` with OS version coarsened to a major. The launch uses `/ping`; `/event` accepts only `mahjong_play` after the first real free-tile move and `newtab_layout` with one of the five fixed layout ids, each at most once per app session. Main applies consent at the trusted IPC boundary, and private tabs never send product events. `installId` is a random per-install UUID in device-local `install.json`, never synced or joined to other Blanc data. The Worker HMACs it before storage/optional GA forwarding, applies strict validation, replay dedup, per-IP/minute and daily caps, and retains only expiring keyed seen-markers plus aggregate counts per metric. A route-level Cloudflare WAF/rate rule and billing alert remain deployment requirements. If the Worker URL changes, update both `PING_ENDPOINT` and `EVENT_ENDPOINT`.

**Blanc Patron and app icons:** Patron unlocks Named Workspace creation; it does not unlock icon variants. macOS Settings offers only Sunrise, Sunrise Dark, Paper, and Ink. `settings.js` exports one `isAppIconAllowed()` predicate that both `setSettings()` (write) and `getSettings()` (read) apply, so retired ids fall back to Sunrise even for an active Patron. The one-time `presentationDefaultsResetVersion` migration intentionally resets existing profiles to Sunrise + Billboard for the next release, stamps the synced layout clock, and then preserves choices made after that reset on all later updates. Patron activation happens in the main process and renderers see only the derived `patronActive` boolean, never the key. Setup runbook: `docs/polar-setup.md`.

**Profile Sync** (`src/main/sync.js` + `src/main/sync-crypto.js`): opt-in, end-to-end-encrypted cross-device sync of **Favorites, settings, and optional per-device open-tab snapshots**. `scrypt`→`HKDF` derives the v1 opaque account locator and AES-256-GCM content key; the passphrase is discarded. The derived key persists only through `safeStorage` wrapping, with Linux `basic_text` rejected; legacy plaintext keys migrate fail-closed. The v1 Worker stores opaque ciphertext in KV and cannot read, index, or merge it. The separately approved authenticated v2 protocol and Durable Object migration belong in their own reviewed project and are not part of this tree. **Never synced:** `supporter`, `appIcon`, `searchSuggestions`, `usagePing`, cookies/storage, history/downloads/permissions, and private tabs. Open-tab icons ride in a separately budgeted encrypted store as bounded source-rasterized PNGs; receivers do not contact remote sites. See `cloudflare/sync-worker/README.md` for the current v1 Worker.

**Address input normalization** (`normalizeAddressInput()` in `main.js`) is a deliberately simple regex heuristic — has-a-scheme, looks-like-localhost, looks-like-a-domain, else treat as a search query against the engine selected in Settings (DuckDuckGo/Google/Bing/Brave). It will misclassify some edge cases (e.g. paths with dots in query strings); that's a known, accepted limitation, not a bug to silently "fix" with a heavier URL parser unless asked. `handOffToOs()` (also in `main.js`) is checked ahead of it wherever a URL becomes a navigation target — page-initiated navigation, `window.open` children, the context menu's "Open Link" actions, and the address bar's raw typed text before `normalizeAddressInput()` ever sees it — so a bare `mailto:`/`tel:`/`facetime:`/`sms:` URI (which has no `://` and would otherwise be misread as a search query or a bogus domain) is handed to the OS instead.

**App icon:** there is no longer one source electron-builder derives everything from — each platform points somewhere different, and **the mark's scale is not portable between them.** macOS packages `build/app-icons/Icon.icon` (`build.mac.icon`), an Icon Composer document whose only artwork is `Assets/blanc-mark.svg`; Windows packages the prebuilt `build/windows-icons/icon-paper.ico` (`build.win.icon`, generated by `scripts/build-windows-icons.js` from `build/icon.png`). `build/icon.png` is now just that 1024×1024 raster template (a byte-copy of `icon-paper.png`), not a thing electron-builder reads directly.

The selectable Dock PNGs in `src/renderer/pages/` — the runtime `dock.setIcon` fallback for unpackaged and pre-macOS-26 runs — share one fixed raster layout: an 824×824 rounded square inset 100px into the 1024 canvas, with the optically strengthened mark in a 544px-tall bounding box. **The Icon Composer source deliberately does NOT share that geometry.** It supplies the mark alone; macOS paints the squircle itself, full-bleed at 1024. So a mark sized to match in *canvas* terms renders ~19% smaller in the Dock — which is exactly how the quit-state bundle icon came to disagree with the running app's. The invariant that actually matters is the mark's share of the **visible tile** (~66%), so the vector mark is scaled by 1024/824 relative to the raster one; Windows compensates for the same mismatch a third way, via `WINDOWS_MARK_SCALE`. A future logo change means re-deriving the mark for each of the three compositions, not copying one geometry across them — `test/unit/app-icon.test.js` fails if the macOS pair drifts apart. `scripts/after-pack-app-icons.js` compiles only Sunrise, Sunrise Dark, Paper, and Ink into `Assets.car`, preserving macOS Default/Dark/Clear/Tinted rendering; Sunrise is the default. Retired colorway source art stays in the repository for possible future reuse but is not selectable, accepted by settings, or packaged into the adaptive catalog.

## Cross-platform parity infrastructure (`spec/`, `tokens/`, `settings-schema/`, `copy/`, `test/desktop/`)

Scaffolding for a **future** native iOS/Android port (landed July 2026, PR #13). The mobile apps don't exist yet — this Electron app is the **reference implementation**, and everything here is additive to it except one test-only, env-gated hook.

**`spec/` is the platform-neutral source of truth for what Blanc *is*.** Features `F1–F24` define the user-facing contract each platform must implement; the divergence register `D1–D14` documents where platforms deliberately differ (e.g. the ad-blocking engine — desktop/Android programmatic vs. iOS's declarative `WKContentRuleList`, which reintroduces a Manifest-V3-style rule cap) plus the parity contract that still holds across the split; `blocking-backends.md` specs the two mobile blocking engines concretely; `parity-matrix.md` is the dashboard; `spec/acceptance/*.feature` are shared Gherkin scenarios. The governing rule: **parity means product/behaviour parity, not implementation parity** — a divergence is *documented* (add a `D#`), not avoided. `spec/README.md` is the entry point. It's a spec, not code; editing it doesn't change the app.

**Three "single source of truth" substrates generate the (future) mobile code *and* guard the desktop against drift.** `tokens/tokens.json` (design tokens), `settings-schema/schema.json` (settings enums + defaults), and `copy/slash-commands.json` (slash-command copy) each have a `build.mjs`: `npm run <name>:build` emits the mobile artifacts under `*/generated/` (Swift/Kotlin, plus iOS `.strings` / Android `.xml`), and `npm run <name>:check` verifies the **desktop** hasn't drifted from that source. `npm run substrate:check` runs all three, plus `adblock:check` — a same-shaped freshness guard that verifies the committed `WKContentRuleList` blocklist (`adblock/generated/`) still matches the pinned EasyList/EasyPrivacy sources (`adblock/` isn't a mobile-codegen substrate, just a generated artifact kept honest the same way); `.github/workflows/parity-guards.yml` runs it (+ the acceptance dry-run) on every push/PR, so drift fails CI.

**The critical operational rule:** the desktop CSS/JS is *guarded, not generated* (a headless build can't visually re-verify chrome), so the source-of-truth JSON and the desktop file are kept in lockstep by the check, not by codegen. If you change a token value in `styles.css`/`pages.css` (the Theming section's `:root` custom properties), a default/enum in `settings.js` (search engines, themes, app icons — Blanc Supporter / App icon sections), or a slash-command hint in `overlay.js`/`pages/shortcuts.js`, you **must** also update the matching JSON and run `<name>:build`, or `substrate:check` fails — and vice versa. Never hand-edit `*/generated/` files. The checkers parse those desktop files with comment-stripping + line-anchored regexes to avoid false-negatives; if you substantially restructure them (single-line vs. one-per-line declarations), re-verify with `node <name>/build.mjs --check`.

**`test/desktop/` is a desktop acceptance harness** (Cucumber + Playwright-Electron) binding the `spec/acceptance/` scenarios to desktop step definitions (116 scenarios executing green in the `runnable` profile, out of 132 total; the rest a tagged backlog). It drives the app through **`src/main/test-hook.js`, a test-only main-process surface `main.js` installs only when `!app.isPackaged && process.env.BLANC_TEST === '1'`** — airtight (never a packaged build; `BLANC_TEST=0/false` stays off). That env-gate is the *only* parity change to shipping code; in test mode it skips the network ad-engine build so the app launches offline and exposes state on `globalThis.__blanc`. `npm run test:acceptance:dry` resolves step definitions without launching; `npm run test:acceptance:desktop` runs them (prefix `xvfb-run -a` on headless Linux; needs the Electron binary).

## Memory benchmark (`bench/memory/`)

A macOS-only harness that measures browser memory, added because an Instagram commenter asked how Blanc compares and nothing in the repo could answer. `npm run bench:memory -- --list` / `--probe` / `--browsers=blanc,chrome,zen --workloads=mixed --reps=3`. It sums **`phys_footprint` across a browser's whole process tree** — never summed RSS, which counts the engine framework once per renderer, so the error scales with process count and systematically penalises whichever engine isolates more per site; that measures the process model twice and calls it a result. Four backends (`footprint`/`vmmap`/`top`/`ps`) are probed in fidelity order and the winner is re-validated against the *first real browser* (`resolveReadableBackend`), because selection can only probe our own unhardened Node process. Results land in `bench/memory/results/`, which is gitignored — commit only a run you actually cite.

**The design rule is that checks fail rather than warn.** A cell must prove it loaded its pages, read back from the browser's own visit log after it quits (`history.json` for Blanc, `Default/History` for Chromium, `places.sqlite` for Gecko), matched **per page not per host**, joined against *visit* tables rather than URL catalogues (a `moz_places` row can be a bookmark), and bounded to the cell's own start time (profiles are copied from a warmed template). A cell with unreadable live processes, no idle baseline, or a browser that never initialised its profile is rejected — never published with a caveat. Three review rounds found 57 issues before it ran; running it found three more that no amount of reading had caught: `kill(pid, 0)` reports a **zombie** as alive, a flat byte total is not settled while the **process tree is still growing**, and macOS *transiently* refuses `footprint` on sandboxed renderers.

**Blanc-specific hazards it encodes, all still true of the app:** `installStartupNavigationGate` (`main.js:226`, installed at `:3751`) cancels every http(s) main-frame request until the blocker compiles, so a cold profile can look perfectly idle while loading nothing; a packaged Blanc copies a legacy `Bowser` userData directory into any path that doesn't exist yet (`main.js:153`), so profile dirs are created *before* launch; an unpackaged Blanc relocates userData to `<dir>-Dev` (`main.js:144`), so never benchmark a dev run; `main.js:3755` opens one `blanc://newtab` beyond the argv URLs (`extraBlankTabs`); and **`BLANC_TEST=1` disables ad blocking**, so the acceptance test-hook is unusable here. Third-party quirks found by running: Zen consumes the *first* startup entry with its own workspace surface (Gecko profiles lead with `about:blank`), Gecko rejects a copied profile carrying a stale `lock`, and a cross-domain redirect (`dailymail.co.uk` → `dailymail.com`) fails per-page matching for Blanc alone because Chromium and Gecko log redirect chains while Blanc records only the committed destination.

**The published figures are pinned.** `site/src/components/MemoryChart.astro` (press + ad-blocking pages), `docs/press/fact-sheet.md`, and the committed run are held together by a `public-truth` test that recomputes the median from the run's own repetitions — change one alone and the build fails. Re-running means updating all three together. The two qualifications must travel with the numbers wherever they appear: Brave is the fair peer (it also blocks by default), and the gap is not only blocking (blocker off, Blanc still beat Chrome).

Marketing-site (`site/`) guidance moved to `site/CLAUDE.md` (loads only when working under `site/`).
