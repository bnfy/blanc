node_modules/
dist/
# Ad-hoc local build output (`--dir=dist-local-…`). `dist/` does not cover these,
# and unpacked electron-builder output runs ~330 MB per directory.
dist-local*
out/
.cache/
*.log
.DS_Store
brand/
.superpowers/
.playwright-cli/
output/

# Local press-outreach drafts (do not commit)
docs/press-release-blanc-desktop.md
docs/press-outreach-plan.md
# Raw benchmark output is machine- and session-specific. Commit the specific
# report you intend to cite in public, not every exploratory run.
bench/memory/results/
.astro/
site/.parity/
site/.parity-shots/
