# v1.11.0 release verification — 2026-08-31

Blanc v1.11.0 was published from source commit
`e3ab5b6712cb7eee8df87df6bcf8c5b1a35b850b` after the complete stable release
gate passed for macOS arm64, Windows, and Linux.

- Release PR: <https://github.com/bnfy/blanc/pull/259>
- Private Windows/Linux validation: <https://github.com/bnfy/blanc/actions/runs/33428605456>
- GitHub release: <https://github.com/bnfy/blanc/releases/tag/v1.11.0>
- Native release workflow: <https://github.com/bnfy/blanc/actions/runs/33431013037>
- Published at: `2026-08-31T19:34:52Z`

## Scope

This release adds the complete Mahjong start-page layout with Daily Peaks as
the default fresh table and Burst gameplay, along with scoring, combos, hints,
shuffle, undo, records, sound controls, responsive layouts, and an optional
hidden footer. It also adds local crash diagnostics and recovery choices,
dedicated certificate-failure UX, the revised Blanc mark across application and
site assets, a verified precompiled blocker seed, MIT licensing and dependency
compliance material, privacy-bounded optional product-use events, Electron
44.1.0, and Windows updater-verification fixes.

## Pre-publication evidence

- The candidate passed all 1,321 unit tests, 127 live desktop acceptance
  scenarios / 781 steps, cold launch, OAuth, DNS, production dependency audit,
  generated-source, branding, token, settings, copy, adblock, compliance,
  Windows-icon, and site-build gates.
- PR #259's required checks passed and GitHub reported the PR mergeable.
- The exact runtime candidate at `ad94b30` passed the private Windows and Linux
  workflow. Both platforms passed artifact, packaged blocker, packaged
  compliance, and hardened-fuse verification; Windows additionally passed the
  exact publisher/timestamp Authenticode gate. The subsequent incident-record
  commit changes documentation only, which is outside electron-builder's
  packaged `build.files` allowlist.
- The signed Windows validation installer
  `Blanc-Setup-1.11.0.exe` had SHA-256
  `52148baedeec6987fc7e97f73160b8974d4c33710795b99a191afd58c2ab83bc`.
  On August 31, the owner installed this exact candidate on Windows and
  confirmed that it installed and worked correctly. This direct candidate
  install is platform runtime evidence, not a public updater handoff.
- The Linux validation AppImage `Blanc-1.11.0.AppImage` had SHA-256
  `7f631680b1ac75ee7abf803f17268bd07e1c1d0f6efc7dc6c728399d391b084c`.
  Its automated package, payload, compliance, and fuse gates passed.

## Linux manual-runtime waiver — APPROVED 2026-08-31

The owner explicitly waived manual Linux runtime testing for v1.11.0 because
Linux is not reliable in the available Parallels Desktop setup. Before the
waiver, the remaining risk was stated: automation can miss an OS-specific
rendering, input, or runtime regression. The owner accepted that bounded risk.
This waiver does not replace the automated Linux gates above or the required
authenticated public AppImage launch/render smoke after publication.

## Publication evidence

- The clean release checkout repeated the complete pre-publication gate: all
  1,321 unit tests, 127 live desktop acceptance scenarios / 781 steps, cold
  launch, OAuth, DNS, production dependency audit, generated-source checks,
  Windows icon checks, and the 18-page site build passed.
- The macOS arm64 app passed Developer ID signing, Apple notarization, strict
  designated-requirement validation, provisioning-profile/WebAuthn checks,
  hardened Electron fuses, byte-identical packaged blocker verification,
  packaged compliance verification, first-run and regression smokes, both live
  favicon matrices, and migration from public Stable v1.10.0.
- The Linux release job passed its tag, artifact, packaged blocker, packaged
  compliance, and hardened-fuse gates before uploading the AppImage and updater
  metadata. GitHub provenance attestation completed.
- The Windows release job passed its tag, icon, blocker-input, artifact,
  packaged blocker, packaged compliance, hardened-fuse, and exact
  publisher/timestamp Authenticode gates before uploading the installer,
  updater metadata, and digest-bound `windows-signature.json`. GitHub
  provenance attestation completed.
- All 11 native and updater inputs were downloaded from the authenticated draft
  into a fresh temporary directory and validated. The resulting CycloneDX
  SBOM and complete `SHA256SUMS` manifest were generated, signed through a fresh
  Safari/GitHub Sigstore authorization for `anthony@bnfy.me` and
  `https://github.com/login/oauth`, and verified before upload.
- The already-verified draft was published as the latest stable release. All 14
  public release assets then passed the logged-out, redirect-following download
  smoke.

## Follow-up evidence

Publication did not by itself prove the adjacent-version macOS or Windows
updater restart handoffs, the authenticated public Linux launch/render smoke,
or the release soak. The Linux gate is now recorded below; the updater
handoffs and any applicable soak remain separate.

### Linux public AppImage launch/render — PASS 2026-08-31

- The repository's `Pre-release smoke` workflow passed against published
  version 1.11.0:
  <https://github.com/bnfy/blanc/actions/runs/33431584386>.
- The Ubuntu 22.04 job downloaded `Blanc-1.11.0.AppImage` and `SHA256SUMS`
  directly from the public release. The AppImage's SHA-256
  `12e1444843a4e0c336697d7f542450817a662af5f5aa06e04f749664330ebc71`
  matched the authenticated manifest, and `gh attestation verify` passed.
- The AppImage launched under Xvfb. Its CDP targets contained
  `blanc-chrome://index/`, `blanc-chrome://overlay/`, and `blanc://newtab/`, and
  the rendered new-tab DOM reported the exact marker `v1.11.0`.
- The same workflow's ordinary Ubuntu and Windows DNS launch smokes also
  passed.
